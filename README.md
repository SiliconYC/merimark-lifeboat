# merimark-lifeboat

每日记（MeriMark）PWA 的应急救生圈。

原则：以正式每日记 PWA 为母版，只做最小差异。正式版的编辑、列表、长按菜单、localStorage 数据结构和 iOS 适配尽量原样保留。

差异只有：不联网同步；“同步”改为“导出 JSON 到剪贴板”；旁边增加小型“清除”按钮并采用确认 + 随机三位数；导出按钮改用暖橙色；静态部署到 GitHub Pages。
